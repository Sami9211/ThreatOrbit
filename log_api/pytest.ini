[pytest]
# Treat Starlette deprecations as errors so the FastAPI TestClient can't
# silently regress to the deprecated httpx shim (httpx2 must be installed; see
# requirements-dev.txt). Also surfaces future Starlette deprecations early.
filterwarnings =
    error::starlette.exceptions.StarletteDeprecationWarning
